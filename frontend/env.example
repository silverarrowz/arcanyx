# Copy to frontend/.env (never commit .env)
# Restart Expo after changing.

# Backend base URL (no trailing slash). See comment below for device vs emulator.
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
