# Copy to backend/.env (never commit .env)

MONGO_URL=mongodb://localhost:27017
DB_NAME=mystix

# Google AI (Gemini) — create key in Google AI Studio
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GEMINI_FALLBACK_MODEL=gemini-2.5-flash
